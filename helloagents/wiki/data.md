# 数据模型

## 概述
本项目不引入业务数据库；仅在需要“防重复执行/重试补偿”时使用 KV 记录每日执行状态。

---

## KV Key（可选）

### `kv:attendance:<sha256(cookie)>:<YYYY-MM-DD>`
**描述:** 标记某个账号在当天（Asia/Shanghai）已成功签到（或已是今日已签状态）。

| 字段 | 类型 | 说明 |
|------|------|------|
| value | boolean | `true` 表示当天已完成 |
